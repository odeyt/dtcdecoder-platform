# Preview validation

Test HTTPS routes `/`, `/catalog`, category/product pages, `/account/login`, callback, protected account, denied admin, robots, sitemap, disabled checkout, order status, webhook existence, denied download, 404, and mobile layout/navigation. Confirm the exact preview callback in Supabase.

Inspect source and browser bundles for secrets/service-role keys. Confirm no public stack traces, redirect loops, mixed content, or runtime errors. Critical failures block production.

